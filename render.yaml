services:
  - type: web
    name: sentinel-nids-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_web:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
